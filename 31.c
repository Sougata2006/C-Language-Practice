#include <stdio.h>
#include <math.h>
int main (){

int side = 3;
printf("the area is %f\n",pow(side,2));
printf("the area is %f\n",pow(side,3));
printf("the area is %f\n",pow(side,4));

    return 0;
}